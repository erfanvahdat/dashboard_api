

import Balance from './Balance.mjs'; // Import the Balance model
// You can import more models here if needed in the future

// Function to sync all models
// const syncAllModels = async () => {
//   try {
//     // Sync the Balance model
//     await Balance.sync({ alter: true }); // Syncs model and alters table if it already exists
//     console.log('Balance model synced successfully.');

//     // Add additional syncs for other models if required
//     // await AnotherModel.sync({ alter: true });

//   } catch (error) {
//     console.error('Error syncing models:', error);
//   }
// };

// export default syncAllModels;
